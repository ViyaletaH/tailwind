export interface Response {
    status: number;
    statusText?: string;
    text?(): Promise<string>;
    json?(): Promise<any>;
    arrayBuffer?(): ArrayBuffer;
}
export type FetchFn = (url: URL, ...args: any[]) => Promise<Response | globalThis.Response>;
/**
 * Wraps a fetch request with retry logic on exceptions, which is useful for
 * spotty connections that may fail intermittently.
 */
export declare function wrapWithRetry(fetch: FetchFn): FetchFn;
